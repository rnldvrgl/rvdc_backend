services:
    api:
        build:
            target: development
        env_file:
            - .env.local
        environment:
            POSTGRES_PASSWORD: ${DB_PASSWORD:-daizuke0908}
        command: ["python", "manage.py", "runserver", "0.0.0.0:8000"]

    db:
        ports:
            - "5432:5432"

