version: '3.8'

services:
  web:
    build: .
    volumes:
      - .:/usr/src/app/
    ports:
      - "8000:8000"

  db:
    image: postgres:15
    environment:
      POSTGRES_DB: rvdc_db
      POSTGRES_USER: rvdc_owner
      POSTGRES_PASSWORD: daizuke0908
